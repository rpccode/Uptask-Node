doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css

    body 
        h2 Confirma Tu Cuenta Uptask
        p Ya estas Casi Listo, Solo preciona el siguiente Boton
        a(href=`${confirmarUrl}`)   Confirmar Cuenta
        p Si no Puedes acceder a este enlace, visita #{confirmarUrl}
        p si no solicitaste este E-mail puede Ignorarlo 